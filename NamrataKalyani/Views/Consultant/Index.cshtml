@model IEnumerable<NamrataKalyani.Models.Consultant>

@{
    ViewBag.Title = "Index";
}

<h2>Counsultant</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Qualification)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Signature)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Department)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Qualification)
        </td>
        <td>
         <img src="@item.Signature" height="40" /> 
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Department)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ConsultantId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ConsultantId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ConsultantId })
        </td>
    </tr>
}

</table>
